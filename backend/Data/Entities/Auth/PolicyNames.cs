namespace Backend.Data.Entities.Auth;

public static class PolicyNames
{
    public const string ResourceOwner = nameof(ResourceOwner);
    public const string ResourceManager = nameof(ResourceManager);
}

